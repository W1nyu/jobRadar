# Checkpoint Archive — M5 완료 — 2026-08-22

M5 완료 직후, M6를 시작하기 전 상태를 보관한다.

- 완료 커밋: `7fbfd65 M5: 키워드 매칭`
- include/exclude, substring·word·regex, target fields와 가중치 순 정렬을 구현했다.
- include와 exclude 양쪽의 `matched_field`, `matched_snippet`, `score`를 저장해 UI에서 근거를
  확인할 수 있다.
- 신규 공고 수집 직후 매칭을 연결하고, 키워드 CRUD API를 `GET/POST/PATCH/DELETE
  /api/v1/keywords`로 제공한다.
- M2의 pg_trgm GIN 인덱스가 title 부분일치 검색을 타는 것을 `EXPLAIN ANALYZE`로 검증했다.

다음 작업은 APScheduler 워커, crawl_runs, advisory lock, 수동 트리거를 추가하는 M6다.
