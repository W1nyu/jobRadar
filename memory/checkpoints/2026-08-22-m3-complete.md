# Checkpoint Archive — M3 완료 — 2026-08-22

M3 완료 직후, M4를 시작하기 전 상태를 보관한다.

- 완료 커밋: `093a3ca M3: 첫 크롤러 2종`
- 공공데이터포털 과기정통부 모집채용 API와 링크어리어 HTML을 각각 `RawJob`으로 수집한다.
- 실제 단발 실행은 과기정통부 10건, 링크어리어 20건을 반환했다.
- 골든 파일, HTTP 재시도·응답 크기 상한·ETag 304, DB-독립 `parse()` 계약을 검증했다.
- 고용24는 개인회원 키로 채용 목록 API를 사용할 수 없어 M7까지 구현을 보류했다.
- 잡알리오도 활용신청/경로를 M7에서 다시 확인한다.

다음 작업은 M4의 `RawJob` 정규화와 PostgreSQL UPSERT, 변경 이력, 3회 연속 미관측 종료 처리다.
