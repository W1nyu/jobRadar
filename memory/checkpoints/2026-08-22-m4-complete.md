# Checkpoint Archive — M4 완료 — 2026-08-22

M4 완료 직후, M5를 시작하기 전 상태를 보관한다.

- 완료 커밋: `cc80637 M4: 신규·중복·변경 탐지`
- `RawJob`을 DTO로 정규화하고, 외부 ID 우선 PostgreSQL UPSERT로 중복을 방지한다.
- 제목·마감일·본문 해시 변경만 revision으로 남기며, 조회수 같은 동적 raw 값은 제외한다.
- `[신입]` 등 접두 태그 변동은 fingerprint에서 흡수한다.
- 완전 수집에서 3회 연속 미관측된 열린 공고만 종료한다. 부분 수집은 오종료를 막기 위해
  종료 판정을 건너뛴다.
- `consecutive_missing_runs` 마이그레이션의 downgrade/upgrade와 M4 DoD를 PostgreSQL에서 검증했다.

다음 작업은 include/exclude·세 가지 매칭 모드·근거 저장을 추가하는 M5 키워드 매칭이다.
