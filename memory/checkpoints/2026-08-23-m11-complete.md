# Checkpoint — M11 완료 시점 — 2026-08-23

## 상태

M0~M11(Azure VM 배포)까지 완료했고, 다음 마일스톤은 M12(운영 안정화·포트폴리오 마감)였다.

- 운영 주소: `https://jobradar.my`
- API·워커 systemd 자동 복구, HTTPS, 외부 DB 포트 차단, PostgreSQL 백업·복구 리허설을 검증했다.
- 운영 카카오 OAuth 토큰 저장·카카오톡 실제 수신과 Web Push 실제 수신을 사용자와 확인했다.
- 관측 시작 전 기준 메모리는 357MiB 사용, Swap 2GiB 중 미사용이었다.

## M11 산출물

`deploy/`에 Ubuntu 초기화, systemd API·워커·백업 유닛, Nginx TLS 구성, 백업·복구 리허설
스크립트를 두었다. `app/api/health.py`의 `/readyz`는 DB까지 확인한다.

M12 시작 전 M10 완료 체크포인트는 `2026-08-23-m10-complete.md`에 보관되어 있다.
