Azure VM에서 24시간 운영할 개인용 **채용공고 모니터링 서비스**를 개발하려고 한다.

프로젝트 목적은 내가 자주 확인하는 여러 기업 및 채용 사이트를 등록해두고, 일정 주기로 새로운 채용공고를 확인한 뒤 내가 설정한 관심 키워드와 일치하는 공고가 새로 등록되면 알림을 제공하는 것이다.

예를 들어 관심 키워드는 다음과 같다.

- 데이터
- 데이터분석
- IT
- 디지털
- AI
- 개발
- 신입
- 인턴

전체적인 동작은 다음과 같다.

```text
채용 사이트
→ 주기적으로 공고 수집
→ 기존 데이터와 비교
→ 신규 공고 탐지
→ 관심 키워드 매칭
→ DB 저장
→ 사용자 알림
```

운영 환경은 다음과 같다.

- Azure VM
- Ubuntu Linux
- 2 vCPU
- RAM 1GB
- 24시간 상시 운영
- 사용자 1명

이 프로젝트는 실제 개인용으로 사용할 예정이지만 **취업용 포트폴리오**에도 활용할 것이다. 따라서 단순 크롤링 스크립트보다는 백엔드, 데이터베이스, 서버 운영, 크롤러 구조, 장애 처리 등을 경험할 수 있는 형태로 설계했으면 한다.

기술 스택은 다음을 우선적으로 고려한다.

- Python
- FastAPI
- PostgreSQL
- SQLAlchemy
- Alembic
- httpx
- BeautifulSoup 또는 lxml
- Scheduler
- Nginx
- systemd

DB는 현재 사용자가 한 명이라 SQLite도 충분할 수 있지만, 포트폴리오 및 실제 DB 서버 운영 경험을 위해 **PostgreSQL 사용을 우선적으로 고려**한다. 다만 RAM 1GB라는 제한이 있으므로 PostgreSQL을 포함한 전체 서비스가 안정적으로 실행될 수 있도록 가볍게 설계한다.

채용 사이트 데이터 수집은 가능하면 다음 순서를 따른다.

```text
공식 API / RSS
→ 내부 JSON API
→ HTTP 요청 + HTML Parsing
→ 필요할 때만 Playwright/Selenium
```

브라우저 자동화는 RAM 사용량이 크기 때문에 최대한 최소화한다.

사이트마다 구조가 다르므로 사이트별 크롤러를 분리하고, 각 크롤러가 공통된 채용공고 데이터 형식으로 결과를 반환하도록 설계한다. 새로운 채용 사이트를 추가할 때 기존 핵심 로직을 크게 수정하지 않는 구조가 좋다.

주요 기능은 다음 정도를 고려한다.

- 모니터링할 채용 사이트 관리
- 관심 키워드 관리
- 신규 채용공고 탐지
- 중복 공고 방지
- 기존 채용공고 변경 여부 탐지
- 주기적인 자동 수집
- 채용공고 조회 페이지
- 크롤링 상태 및 오류 확인
- 관심 공고 알림

알림은 Telegram이나 Discord는 사용하지 않는다.

다음 방식을 우선 검토한다.

- Web Push / 브라우저 알림
- 카카오톡을 활용할 수 있는 공식 API가 적절하다면 카카오톡 개인 알림

관리 화면은 초기에는 FastAPI + Jinja2 정도의 간단한 웹 페이지로 구성해도 된다.

프로젝트 규모에 비해 과도한 기술은 사용하지 않는다.

특별한 이유가 없다면 다음은 사용하지 않는다.

- Kubernetes
- Microservices
- Kafka
- RabbitMQ
- Redis
- Celery
- Elasticsearch

이 프로젝트는 반드시 **Milestone 방식**으로 구현한다.

```text
M1 → M2 → M3 → M4 → ...
```

처럼 기능 단위로 구현 단계를 나누고 순차적으로 진행한다.

기간 기준이 아니라 **기능 완성 기준으로 Milestone을 구성**한다.

예를 들면 다음과 같은 방향을 참고할 수 있지만, 실제 Milestone 구성은 프로젝트를 분석한 뒤 더 적절하게 설계해도 된다.

```text
M1 → 프로젝트 기본 환경 구성
M2 → PostgreSQL 및 데이터 모델
M3 → 첫 번째 채용 사이트 크롤러
M4 → 신규/중복 공고 탐지
M5 → 키워드 매칭
M6 → Scheduler 자동 수집
M7 → 다중 사이트 지원
M8 → 웹 관리 페이지
M9 → 알림 기능
M10 → 장애 처리 및 테스트
M11 → Azure VM 배포 및 운영
```

처음에는 바로 코드를 구현하지 말고 먼저 다음을 정리해줘.

1. 프로젝트의 구체적인 기능 요구사항
2. 적절한 기술 스택
3. 전체 시스템 아키텍처
4. PostgreSQL 사용 여부와 이유
5. 주요 DB 구조
6. 크롤러 구조
7. 알림 방식
8. Azure VM 2 vCPU / RAM 1GB 환경에서의 운영 전략
9. 최종 Milestone 구성

그 다음 실제 개발은 반드시 **M1 → M2 → M3 → ...** 순서로 진행해줘.